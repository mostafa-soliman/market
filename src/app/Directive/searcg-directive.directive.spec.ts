import { SearcgDirective } from './searcg-directive.directive';

describe('SearcgDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SearcgDirective();
    expect(directive).toBeTruthy();
  });
});
